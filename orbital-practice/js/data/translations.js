// =============================================================================
// Translations Module — English strings
// =============================================================================

import { enUI } from "./locales/ui/en.js";

/**
 * English-only UI string table, keyed by language code for compatibility
 * with the lookup shape langController.js expects.
 */
export const translations = {
  "en": enUI,
};
