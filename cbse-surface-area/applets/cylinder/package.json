{
  "name": "surface-area-cylinder",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "../node_modules/.bin/esbuild src/main.jsx --bundle --outfile=assets/js/bundle.js --format=iife --loader:.jsx=jsx --jsx=automatic --define:process.env.NODE_ENV=\\\"production\\\" --minify"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
